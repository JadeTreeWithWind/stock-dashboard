import { Button } from "@/components/ui/button.tsx";

const Home = () => {
  return (
    <div className="flex h-screen items-center justify-center">
      <Button>Home</Button>
    </div>
  );
};
export default Home;
